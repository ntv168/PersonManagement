<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimaryDark">#339A99</item>
        <item name="colorAccent">@color/white</item>
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/actionbarStyle</item>
        <item name="android:fontFamily">Roboto</item>
    </style>

    <style name="ButtonStyle">
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">#000</item>
    </style>

    <style name="actionbarStyle" parent="@android:style/Widget.Holo.ActionBar">
        <item name="android:background">@color/white</item>
        <item name="android:titleTextStyle">@style/textcolorStyle</item>
    </style>

    <style name="textcolorStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">#339A99</item>
    </style>

    <style name="layouttheme" parent="AppTheme">
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
